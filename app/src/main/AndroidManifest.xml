<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  package="net.ddns.satsukies.transition_tuning"
  >

  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    android:name=".MyApplication"
    >
    <meta-data
      android:name="com.bumptech.glide.integration.okhttp3.OkHttpGlideModule"
      tools:node="remove"
      />

    <activity android:name=".MenuActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".normal.NormalMasterActivity" />
    <activity android:name=".normal.NormalDetailActivity" />
    <activity android:name=".advanced.AdvancedMasterActivity" />
    <activity android:name=".advanced.AdvancedDetailActivity" />
    <activity android:name=".shared.SharedMasterActivity" />
    <activity android:name=".shared.SharedDetailActivity" />
  </application>

</manifest>